type User {
    id: ID! @id
    name: String!
    uid: String! @unique
    robot: Boolean! @default(value: false)
    settings: UserSettings!
    registeredAt: DateTime! @createdAt
}

type UserSettings @embedded{
    favoriteNews: [News]! @relation(link: INLINE, name: "USER_FAVORITE_NEWS")
    memo: String!
}
